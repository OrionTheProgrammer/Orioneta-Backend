<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.14</version>
        <relativePath/>
    </parent>

    <groupId>cl.orioneta</groupId>
    <artifactId>orioneta-backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>orioneta-backend</name>
    <description>Backend de Orioneta basado en microservicios</description>
    <packaging>pom</packaging>

    <modules>
        <module>shared/shared-kernel</module>
        <module>shared/shared-events</module>
        <module>shared/shared-security</module>
        <module>shared/shared-observability</module>

        <module>gateway-service</module>
        <module>bff-service</module>
        <module>auth-service</module>
        <module>user-service</module>
        <module>friendship-service</module>
        <module>conversation-service</module>
        <module>message-service</module>
        <module>realtime-service</module>
        <module>notification-service</module>
        <module>customization-service</module>
        <module>neta-market-service</module>
        <module>media-service</module>
        <module>moderation-service</module>
        <module>audit-service</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <spring-cloud.version>2025.0.0</spring-cloud.version>
        <springdoc.version>2.8.9</springdoc.version>
        <minio.version>8.5.17</minio.version>
        <jacoco.version>0.8.14</jacoco.version>
        <sonar.maven.plugin.version>5.1.0.4751</sonar.maven.plugin.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <sonar.projectKey>orioneta-backend</sonar.projectKey>
        <sonar.projectName>orioneta-backend</sonar.projectName>
        <sonar.coverage.jacoco.xmlReportPaths>
            target/site/jacoco/jacoco.xml
        </sonar.coverage.jacoco.xmlReportPaths>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.sonarsource.scanner.maven</groupId>
                    <artifactId>sonar-maven-plugin</artifactId>
                    <version>${sonar.maven.plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
